{
  "name": "@starter/backend",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts",
    "./services/*": "./src/services/*.ts",
    "./services/auth/*": "./src/services/auth/*.ts",
    "./services/auth/user/*": "./src/services/auth/user/*.ts",
    "./types/*": "./src/types/*.ts",
    "./utils/*": "./src/utils/*.ts"
  },
  "scripts": {
    "db:start": "bunx supabase start",
    "db:stop": "bunx supabase stop",
    "db:reset": "bunx supabase db reset --local && bun run db:migrate && bun run db:seed",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:migrate:prod": "sh -c 'set -a && . ./.env.prod && bunx drizzle-kit migrate'",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio",
    "db:seed": "bun run src/seed.ts",
    "typecheck": "bunx tsc"
  },
  "devDependencies": {
    "@starter/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@starter/logger": "workspace:*",
    "@supabase/supabase-js": "catalog:",
    "better-auth": "catalog:",
    "date-fns": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "postgres": "catalog:",
    "uuid": "catalog:",
    "zod": "catalog:"
  }
}
